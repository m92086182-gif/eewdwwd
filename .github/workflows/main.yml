name: Windows RDP Server (Public)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'مدة التشغيل (ساعات)'
        required: true
        default: '72'
        type: choice
        options:
          - '24'
          - '48'
          - '72'
          - '168'

env:
  CUSTOM_PORT: 3389
  USERNAME: 'AdminRDP'

jobs:
  deploy-public-rdp:
    runs-on: windows-latest
    timeout-minutes: ${{ fromJson('{"24": 1440, "48": 2880, "72": 4320, "168": 10080}[github.event.inputs.duration] || 4320') }}
    
    steps:
    - name: التحقق من المواصفات
      run: |
        echo "🖥️ مواصفات السيرفر:"
        systeminfo | findstr /C:"Total Physical Memory"
        systeminfo | findstr /C:"Processor(s)"
        echo "💾 المساحة المتاحة:"
        fsutil fsinfo drives

    - name: تعطيل إعدادات الأمان المؤقتة للـRDP
      run: |
        # تفعيل RDP
        Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0 -Force
        
        # تعطيل NLA مؤقتاً للاتصال السهل
        Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0 -Force
        
        # إعدادات الجلسات المتعددة
        Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" -Name "fSingleSessionPerUser" -Value 0 -Force

    - name: فتح منافذ RDP في الجدار الناري
      run: |
        # حذف أي قواعد موجودة مسبقاً
        netsh advfirewall firewall delete rule name="PublicRDP" 2>&1 | Out-Null
        
        # فتح منفذ RDP الافتراضي
        netsh advfirewall firewall add rule name="PublicRDP" dir=in action=allow protocol=TCP localport=$env:CUSTOM_PORT
        
        # إظهار القواعد المضافَة
        netsh advfirewall firewall show rule name="PublicRDP"

    - name: إنشاء مستخدم مسؤول بكلمة مرور قوية
      run: |
        # إنشاء كلمة مرور قوية عشوائية
        Add-Type -AssemblyName System.Web
        $password = [System.Web.Security.Membership]::GeneratePassword(16, 4)
        $securePassword = ConvertTo-SecureString $password -AsPlainText -Force
        
        # حذف المستخدم إذا كان موجوداً مسبقاً
        try {
            Remove-LocalUser -Name $env:USERNAME -ErrorAction SilentlyContinue
        } catch { }
        
        # إنشاء المستخدم الجديد
        New-LocalUser -Name $env:USERNAME -Password $securePassword -AccountNeverExpires
        Add-LocalGroupMember -Group "Administrators" -Member $env:USERNAME
        Add-LocalGroupMember -Group "Remote Desktop Users" -Member $env:USERNAME
        
        # تفعيل حساب Administrator المدمج
        Enable-LocalUser -Name "Administrator"
        $adminPassword = [System.Web.Security.Membership]::GeneratePassword(16, 4)
        $adminSecurePassword = ConvertTo-SecureString $adminPassword -AsPlainText -Force
        Set-LocalUser -Name "Administrator" -Password $adminSecurePassword
        
        # حفظ بيانات الدخول في متغيرات الآمنة
        echo "RDP_USERNAME=$env:USERNAME" | Out-File -FilePath $env:GITHUB_ENV -Append
        echo "RDP_PASSWORD=$password" | Out-File -FilePath $env:GITHUB_ENV -Append
        echo "ADMIN_PASSWORD=$adminPassword" | Out-File -FilePath $env:GITHUB_ENV -Append

    - name: الحصول على IP العام وتعيين الاسم
      run: |
        # الحصول على IP العام
        $publicIP = (Invoke-RestMethod -Uri "https://api.ipify.org").Trim()
        echo "PUBLIC_IP=$publicIP" | Out-File -FilePath $env:GITHUB_ENV -Append
        
        # تعيين اسم جهاز مميز
        $hostname = "GH-RDP-$env:GITHUB_RUN_ID"
        Rename-Computer -NewName $hostname -Force
        echo "COMPUTER_NAME=$hostname" | Out-File -FilePath $env:GITHUB_ENV -Append

    - name: تحسين أداء Windows للسيرفر
      run: |
        # تحسين إعدادات الأداء
        powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c  # High performance
        
        # تعطيل السكون
        powercfg -change -standby-timeout-ac 0
        powercfg -change -hibernate-timeout-ac 0
        
        # تحسين إعدادات RDP
        Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" -Name "MaxInstanceCount" -Value 20

    - name: تثبيت البرامج الأساسية
      run: |
        # تحميل وتثبيت Google Chrome
        $chromeInstaller = "$env:TEMP\chrome_installer.exe"
        try {
            Invoke-WebRequest "https://dl.google.com/chrome/install/latest/chrome_installer.exe" -OutFile $chromeInstaller
            Start-Process -FilePath $chromeInstaller -Args "/silent /install" -Wait
            Remove-Item $chromeInstaller -Force
        } catch {
            Write-Host "⚠️ فشل تثبيت Chrome"
        }

    - name: تشغيل وإعداد الخدمات
      run: |
        # إعادة تشغيل خدمات RDP
        Restart-Service -Name "TermService" -Force
        Start-Sleep -Seconds 5

    - name: اختبار الاتصال وعرض المعلومات
      run: |
        # اختبار منفذ RDP محلياً
        $testResult = Test-NetConnection -ComputerName localhost -Port $env:CUSTOM_PORT -InformationLevel Quiet
        
        # عرض معلومات الاتصال
        Write-Host ""
        Write-Host "=" * 60
        Write-Host "🎯 RDP السيرفر جاهز للاستخدام!"
        Write-Host "=" * 60
        Write-Host "🌐 IP العام: $env:PUBLIC_IP"
        Write-Host "🚪 المنفذ: $env:CUSTOM_PORT"
        Write-Host "👤 المستخدم الرئيسي: $env:RDP_USERNAME"
        Write-Host "🔑 كلمة المرور: $env:RDP_PASSWORD"
        Write-Host "👑 المسؤول: Administrator"
        Write-Host "🔑 كلمة مرور المسؤول: $env:ADMIN_PASSWORD"
        Write-Host "💻 اسم الجهاز: $env:COMPUTER_NAME"
        Write-Host "⏰ المدة المتبقية: ${{ github.event.inputs.duration }} ساعة"
        Write-Host "=" * 60
        Write-Host "📝 للاتصال: mstsc → $env:PUBLIC_IP:$env:CUSTOM_PORT"
        Write-Host "=" * 60
        Write-Host ""

    - name: نظام المراقبة والتجديد التلقائي
      run: |
        $endTime = (Get-Date).AddHours(${{ github.event.inputs.duration }})
        $checkInterval = 300 # 5 دقائق
        $counter = 0
        
        while ((Get-Date) -lt $endTime) {
            $timeLeft = $endTime - (Get-Date)
            $hoursLeft = [math]::Round($timeLeft.TotalHours, 1)
            
            # تحديث معلومات الحالة كل 30 دقيقة
            if ($counter % 6 -eq 0) {
                Write-Host "[$(Get-Date)] ✅ السيرفر يعمل - $hoursLeft ساعة متبقية"
                Write-Host "🌐 IP: $env:PUBLIC_IP - 👤 User: $env:RDP_USERNAME"
            }
            
            # اختبار الاتصال المحلي
            try {
                $test = Test-NetConnection -ComputerName localhost -Port $env:CUSTOM_PORT -WarningAction SilentlyContinue
                if (-not $test.TcpTestSucceeded) {
                    Write-Host "⚠️  إعادة تفعيل RDP..."
                    Restart-Service -Name "TermService" -Force
                    Start-Sleep -Seconds 10
                }
            } catch {
                Write-Host "⚠️  خطأ في الاختبار، إعادة التشغيل..."
                Restart-Service -Name "TermService" -Force
                Start-Sleep -Seconds 10
            }
            
            # انتظار حتى الفحص التالي
            Start-Sleep -Seconds $checkInterval
            $counter++
        }
        
        Write-Host "⏰ انتهت المدة - إيقاف السيرفر"

    - name: التنظيف النهائي
      if: always()
      run: |
        Write-Host "🧹 جاري التنظيف..."
        # تعطيل RDP
        Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 1 -Force
        
        # حذف قواعد الجدار الناري
        netsh advfirewall firewall delete rule name="PublicRDP" 2>&1 | Out-Null
        
        # حذف المستخدمين
        try {
            Remove-LocalUser -Name $env:USERNAME -ErrorAction SilentlyContinue
        } catch { }
        
        Write-Host "✅ تم التنظيف بنجاح"
